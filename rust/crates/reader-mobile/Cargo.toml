[package]
name = "universal-reader-mobile"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "C ABI for Universal Reader mobile format conversion."

[lib]
name = "universal_reader_native"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
universal-reader-format-native = { path = "../reader-format-native" }
